<!DOCTYPE html>
<html lang="en">

<head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>
        <%= item.name %>
    </title>
    <style>
        .comment{
            border-radius: 15px;
            box-shadow: 3px 3px 10px rgb(194, 194, 194);
            margin: 10px;
            padding: 15px;
            background-color: rgb(218, 193, 170);
        }
        
        body {
            background-color: rgb(238, 218, 191);
            text-align: center;
        }

        .custom-card {
            text-align: left;
            border-radius: 15px;
            box-shadow: 3px 3px 10px rgb(194, 194, 194);
            margin: 10px;
            padding: 10px;
            background-color: rgb(250, 243, 227);

        }

        .comment-name {
            font-weight: bold;
            font-size: 23px;
        }

        .paragraph {
            font-size: larger;
        }

        .main {
            display: grid;
            grid-template-columns: 900px 400px;
        }
    </style>
</head>

<body>
    <a class="btn btn-success" href="/index"> Back to Smoothies List </a>
    <br>
    <div class="main">
        <div class="custom-card">
            <img width=500 src="<%= item.photo %> ">
            <h1 class="display-4">
                <%= item.name %>
            </h1>
            <span class="display-6"> total: <%= item.total %> min 🕐 </span>
            <span class="display-6"> level:<%= item.level %> ⚡ </span>
            <h1 class="display-6">Ingredients:</h1>
            <p class="paragraph">
                <%= item.ingredient %>
            </p>
            <h1 class="display-6">Direction:</h1>
            <p class="paragraph">
                <%= item.direction %>
            </p>
            <a class="btn btn-primary" href="/edit/<%= id %> "> Edit </a>
            <a class="btn btn-danger" href="/delete/<%= id %>  "> Delete </a>
           
        </div>

        <div class="custom-card" style="text-align: center;">
            <% for(let i=0 ; i < item.comments.length ; i++ ) { %>
                <div class="comment" style="text-align: left;">
                    <h1 class="display-6 comment-name">
                        <%= item.comments[i].user %>
                    </h1 >
                    <p class="paragraph">
                        <%= item.comments[i].text %>
                    </p>
                </div>
                <% } %>
                <a class="btn btn-secondary" href="/comment/<%= id %> "> Comment </a>
        </div>
    </div>

</body>

</html>